# firmware images (large, not for VCS)
*.img
# python
.venv/
__pycache__/
*.pyc
# build artifacts
*.o
build/
# macOS / editor cruft
.DS_Store
# local Claude Code config (permissions allowlist — not for VCS)
.claude/
