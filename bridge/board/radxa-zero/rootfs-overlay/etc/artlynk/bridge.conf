# ArtLynk bridge runtime config (sourced by the init scripts + artlynk-view).
GOGGLE_IP=10.55.0.1        # the goggle's venc8 tap
GOGGLE_PORT=9000
HOST_CIDR=10.55.0.2/24     # the IP we give ourselves on the USB-ECM link

# decode path:  hw = Amlogic V4L2 stateless HEVC (low latency)  |  sw = avdec_h265 (fallback, M2)
DECODE=hw
